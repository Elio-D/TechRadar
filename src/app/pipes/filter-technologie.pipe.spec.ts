import { FilterTechnologiePipe } from './filter-technologie.pipe';

describe('FilterTechnologiePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTechnologiePipe();
    expect(pipe).toBeTruthy();
  });
});
